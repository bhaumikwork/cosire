<%= render 'refinery/admin/search_header', :url => refinery.strategies_admin_strategies_path %>
<div class='pagination_container'>
  <% if @strategies.any? %>
    <%= render 'strategies' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
