<%= render 'refinery/admin/search_header', :url => refinery.packages_admin_packages_path %>
<div class='pagination_container'>
  <% if @packages.any? %>
    <%= render 'packages' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
