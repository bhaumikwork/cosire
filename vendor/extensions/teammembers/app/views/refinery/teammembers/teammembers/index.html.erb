<% content_for :body do %>
  <ul id="teammembers">
    <% @teammembers.each do |teammember| %>
      <li>
        <%= link_to teammember.name, refinery.teammembers_teammember_path(teammember) %>
      </li>
    <% end %>
  </ul>
<% end %>

<%= render '/refinery/content_page' %>
