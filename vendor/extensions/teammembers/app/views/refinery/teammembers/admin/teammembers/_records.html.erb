<%= render 'refinery/admin/search_header', :url => refinery.teammembers_admin_teammembers_path %>
<div class='pagination_container'>
  <% if @teammembers.any? %>
    <%= render 'teammembers' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
