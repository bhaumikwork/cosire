<%= render 'refinery/admin/search_header', :url => refinery.ips_admin_ips_path %>
<div class='pagination_container'>
  <% if @ips.any? %>
    <%= render 'ips' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
