<%= render 'refinery/admin/search_header', :url => refinery.digitals_admin_digitals_path %>
<div class='pagination_container'>
  <% if @digitals.any? %>
    <%= render 'digitals' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
