<% content_for :body do %>
  <ul id="achievements">
    <% @achievements.each do |achievement| %>
      <li>
        <%= link_to achievement.title, refinery.achievements_achievement_path(achievement) %>
      </li>
    <% end %>
  </ul>
<% end %>

<%= render '/refinery/content_page' %>
